<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">

<properties>
	
	<!-- 회원가입 -->
	<entry key="signUp">
		INSERT INTO MEMBER
		VALUES(SEQ_MEMBER_NO.NEXTVAL, ?, ?, ?, ?, DEFAULT, DEFAULT, DEFAULT, DEFAULT)
	</entry>
	
	<!-- 아이디 중복 확인 -->
	<entry key="idDupCheck">
		SELECT COUNT(*) FROM MEMBER
		WHERE STATUS_CD != 2
		AND MEMBER_ID = ?
	</entry>
	
	<!-- 이메일 중복 확인 -->
	<entry key="emailDupCheck">
		SELECT COUNT(*) FROM MEMBER
		WHERE STATUS_CD != 2
		AND MEMBER_EMAIL = ?
	</entry>
	
	<!-- 로그인 -->
	<entry key="login">
		SELECT MEMBER_NO, MEMBER_NM, MEMBER_EMAIL,
        ENROLL_DT, STATUS_CD, GRADE_CD
		FROM MEMBER
		WHERE MEMBER_ID = ?
		AND MEMBER_PW = ?
		AND STATUS_CD != 2
	</entry>
	
	<entry key="certification">
		SELECT COUNT(*)
		FROM MEMBER
		WHERE MEMBER_ID=?
		AND MEMBER_EMAIL=?
		AND STATUS_CD != 2
	</entry>
	
	<!-- 비밀번호 변경 -->
	<entry key="pwUpdate">
		UPDATE MEMBER 
		SET MEMBER_PW = ?
		WHERE MEMBER_ID = ?
		AND MEMBER_EMAIL = ?
		AND status_cd != 2
	</entry>
	
	<!-- 반려동물 카테고리 목록 조회 -->
	<entry key="selectAnimalCategory">
		SELECT * FROM ANIMAL_CATEGORY
	</entry>
	
	<!-- 반려동물 리스트 -->
	<entry key="selectanimalList">
		SELECT ANIMAL_NO,ANIMAL_NAME,ANIMAL_VARIETY,ANIMAL_SEX,ANIMAL_BIRTH,ANIMAL_CATEGORY_CD,ANIMAL_CATEGORY_NM,MEMBER_NO
		FROM ANIMAL
		JOIN ANIMAL_CATEGORY USING(ANIMAL_CATEGORY_CD)
		WHERE MEMBER_NO = ?
	</entry>
	
	<entry key="addAnimal">
	INSERT INTO ANIMAL VALUES(?,?,?,?,?,?,?)
	</entry>
	
	<!-- 반려동물 다음 번호 조회 -->
	<entry key="nextAnimalNo">
		SELECT SEQ_ANIMAL_NO.NEXTVAL FROM DUAL
	</entry>
	
	<!-- 반려동물 프로필 삽입 -->
	<entry key="insertaniProfile">
		INSERT INTO ANIMAL_IMG VALUES(SEQ_ANIMAL_IMG_NO.NEXTVAL,?,?,?,?)
	</entry>
	

</properties>